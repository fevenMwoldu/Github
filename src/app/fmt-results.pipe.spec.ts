import { FmtResultsPipe } from './fmt-results.pipe';

describe('FmtResultsPipe', () => {
  it('create an instance', () => {
    const pipe = new FmtResultsPipe();
    expect(pipe).toBeTruthy();
  });
});
